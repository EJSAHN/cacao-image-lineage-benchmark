# Repository checkout and persistent analysis results.
export CACAO_PROJECT_ROOT="$PWD/work"

# Large public archives, extracted images, embeddings and model caches.
export CACAO_WORKING_ROOT="$PWD/work-cache"

# Optional explicit 7-Zip executable for RAR archives.
export CACAO_RAR_TOOL="$(command -v 7zz || true)"
