# Derived data

`derived/` contains the summary workbook, complete 14,601-row compressed photograph manifest, and principal CSV/TSV outputs. The workbook and compressed manifest must be distributed together. The workbook includes stored matched-removal replicate metrics; it does not contain raw photographs. No source photographs, annotations, embeddings, or model checkpoints are redistributed.
